'use strict';

angular.module('2ViVe')
  .controller('CareerController', function($scope) {
  	/*
    $scope.scrollTo = function(id) {
       $location.hash(id);
       $anchorScroll();
    };
    */
});
